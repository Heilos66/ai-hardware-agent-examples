# ============================================================
# ConvAI 运行时配置文件 (Windows 模拟器)
#
# 用法:
#   1. 将本文件复制为 convai.cfg
#   2. 与 goldieos.exe 放在同一目录
#   3. 删除对应行首的 # 并填入真实值后启动模拟器
#
# 格式: 每行一个 键=值, 等号两边不能有空格, 值内不能包含空格
#       行首 # 或 ; 为注释
#
# 生效规则: 仅未注释的键覆盖编译期默认值 (convai_bridge_defaults.c);
#           全部注释时程序使用编译进固件的默认凭证
# ============================================================

# ---- 产品凭证 (从华为云控制台获取) ----
#product_id=your_product_id
#product_key=your_product_key
#product_secret=your_product_secret
#agent_id=your_agent_id